# Contactbook CLI
This contactbook CLI was made for Resolution Python week 2.


## Commands
```python main.py [name]``` will add a contact with the given name.

```python main.py [name] [email]``` will add a contact with the given name and email.

```python main.py -d [id]``` will delete a contact by ID.

```python main.py -l```will list all contacts.